version: "3.9"
services:
  oracle:
    build: .
    container_name: antigravity_oracle
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - oracle_data:/app/data

volumes:
  oracle_data:
